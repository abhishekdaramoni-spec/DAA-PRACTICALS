## 📌 Summary

This project solves the **Coin Change Problem** using Dynamic Programming. It finds the minimum number of coins needed to make a given amount and also measures the execution time.

## 🎯 Conclusion

Dynamic Programming makes the solution efficient by storing and reusing previously calculated results. The algorithm has **O(n × amount) time complexity** and **O(amount) space complexity**.
